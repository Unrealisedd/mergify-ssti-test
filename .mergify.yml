pull_request_rules:
  - name: base-marker
    conditions: [base=main]
    actions:
      comment:
        message: BASE-CONFIG-EXECUTED
  - name: head-injected
    conditions: [base=main]
    actions:
      comment:
        message: HEAD-CONFIG-EXECUTED-CRITICAL
