pull_request_rules:
  - name: gs-merge
    conditions:
      - label=gs-merge
    actions:
      merge:
        method: squash
        commit_message_template: |
          chore: automated squash

          INJ-START
          {{ body | get_section("## Commit") }}
          INJ-END
